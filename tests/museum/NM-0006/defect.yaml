id: NM-0006
symptom: "FlattenedAvroParser rewrites cust_addr__city into the dotted path cust.addr.city, and results were keyed by that path -- so a caller looking up their own column name got a KeyError from a result set that did contain the field"
severity: HIGH
fixed_in: 7be3d1d
gate_that_should_have_caught_it: "none -- every matching test built SchemaFields by hand with dotted paths, so no test ever asked for a result under the name a flattened export actually uses"
gate_that_catches_it_now: "tests/museum/NM-0006/test_nm_0006.py, plus tests/unit/application/test_match_result_identity.py"
touches:
  - src/nexus_matcher/application/use_cases/match_schema.py
